apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
 - ../base/harbor # path to the base service

patches:
  - path: harbor-values.yaml # looks for the file in this directory
    target:
      kind: HelmRelease
      name: harbor # patches the additional values to this helmRelease